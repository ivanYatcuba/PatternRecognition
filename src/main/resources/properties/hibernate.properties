#H2 settings
h2.driver = org.h2.Driver
h2.dialect = org.hibernate.dialect.H2Dialect
h2.url = jdbc:h2:~/app
h2.username = sa

#Hibernate Configuration:
hibernate.show_sql=true
hibernate.hbm2ddl.auto=update
entitymanager.packages.to.scan=app.backend.model
